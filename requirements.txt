aiogram>=3.15,<4
websockets>=13.0
aiohttp>=3.10
# нужен aiogram для работы Telegram через прокси (TELEGRAM_PROXY)
aiohttp-socks>=0.9
python-dotenv>=1.0

# нужен только для проверки платного лицензионного ключа;
# в бесплатном режиме мост работает и без него
cryptography>=42.0

# QR-логин: рендер кода в консоль (login --qr) и в PNG для бота-онбординга.
# pypng даёт PNG без тяжёлого Pillow (чистый Python).
qrcode>=7.4
pypng>=0.20
